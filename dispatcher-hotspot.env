# Hotspot Configuration for NetworkManager Dispatcher Script
#
# This file is used by the dispatcher script at:
# /etc/NetworkManager/dispatcher.d/70-wifi-wired-exclusive.sh
#
# Installation:
#   sudo cp dispatcher-hotspot.env /etc/NetworkManager/dispatcher.d/hotspot.env
#   sudo chmod 600 /etc/NetworkManager/dispatcher.d/hotspot.env
#
# IMPORTANT: The HOTSPOT_CON_NAME must match HOTSPOT_CONNECTION_NAME
# in the dispatcher script (default: auto-hotspot)

# Connection name in NetworkManager (must match dispatcher script)
HOTSPOT_CON_NAME=auto-hotspot

# WiFi network name (SSID) that clients will see
HOTSPOT_SSID=AutoHotspot

# WiFi password (minimum 8 characters, 10+ recommended)
HOTSPOT_PASSWORD=examplecomplicatedpassword

# WiFi interface (leave empty for auto-detection)
# Example: wlp4s0, wlan0
WIFI_INTERFACE=

# Wired ethernet interface (leave empty for auto-detection)
# Example: enp3s0, eth0
WIRED_INTERFACE=
